<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Diagnosis Details</title>
    <link rel="stylesheet" href="/style.css" />
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(to right, #e0f7fa, #ffffff);
            margin: 0;
            padding: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        header {
            background: linear-gradient(to right, #0077b6, #0096c7);
            color: white;
            padding: 1.5rem;
            text-align: center;
            width: 100%;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
        }

        form {
            margin: 1.2rem;
            width: 100%;
            display: flex;
            justify-content: center;
        }

        button {
            padding: 0.75rem 1.5rem;
            background-color: #00b4d8;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s ease;
            width: 200px;
        }

        button:hover {
            background-color: #0077b6;
        }

        a {
            text-decoration: none;
            margin-top: 1rem;
            font-weight: bold;
            color: #0077b6;
            font-size: 1rem;
        }
    </style>
</head>
<body>
    
    <header>
        <h1>Your Diagnosis</h1>
    </header>
    
    <form method="post" action="/bloodTest">
        <button>Blood Test</button>
    </form>

    <form method="post" action="/diabetesTest">
        <button>Diabetes Test</button>
    </form>

    <a href="http://localhost:8080/user">back</a>

</body>
</html>