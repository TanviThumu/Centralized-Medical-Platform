<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Precription Details</title>
    <link rel="stylesheet" href="/style.css" />
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(to right, #e0f7fa, #ffffff);
            margin: 0;
            padding: 0;
            min-height: 100vh;
        }

        header {
            background: linear-gradient(to right, #0077b6, #0096c7);
            color: white;
            padding: 1.5rem;
            text-align: center;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
        }

        ul {
            list-style-type: none;
            padding: 0;
            margin: 2rem auto;
            max-width: 500px;
            background: #ffffff;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            padding: 1rem 2rem;
        }

        li {
            padding: 0.5rem 0;
            border-bottom: 1px solid #e0e0e0;
            font-size: 1.1rem;
        }

        li:last-child {
            border-bottom: none;
        }

        form, a {
            display: flex;
            justify-content: center;
            margin: 1.5rem;
        }

        button {
            padding: 0.7rem 1.5rem;
            background-color: #00b4d8;
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1rem;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #0077b6;
        }

        a {
            text-decoration: none;
            color: #0077b6;
            font-weight: bold;
            font-size: 1rem;
        }
    </style>
</head>
<body>
    
    ]<header>
        <h1>Your Prescriptions</h1>
    </header>
    <ul>
        <% prescriptions.forEach(p => { %>
        <li><%= p.tabletName %> - <%= p.dosage %></li>
        <% }) %>
    </ul>

    <form method="get" action="/newPrescription">
        <button>Add new prescription</button>
    </form>

    <a href="http://localhost:8080/user">back</a>

</body>
</html>